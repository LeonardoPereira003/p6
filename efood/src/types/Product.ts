// Tipagem de produto (API e Carrinho)
export type Product = {
    id: number
    nome: string
    descricao: string
    descricaoLonga: string 
    foto: string
    preco: number
    porcao: string
}
